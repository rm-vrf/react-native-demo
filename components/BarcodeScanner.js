import * as React from 'react';
import { Text, Button } from 'react-native';

const BarcodeScanner = () => {
    return <Text>This is barcode/QR code scanner screen. Show how to scan barcode/QR code and get result (barcode type and value) from native method.</Text>;
};

export default BarcodeScanner;